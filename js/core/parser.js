// js/core/parser.js — Flat XSD DOM → SchemaElement tree.
//
// See web-implementation-spec.md §4 (Schema Parsing), §5 (Packet Analysis), and
// §8 (Path Conventions — the shared joinPath helper and other path predicates
// used across parser/xmlWriter/xmlReader are expected to live here). Implemented
// in IMPLEMENTATION_PLAN.md Phase 1.2 (parsing) and Phase 1.3 (packet analysis).
//
// Pure logic, no DOM dependencies (other than reading the flat XSD DOM).

export {};
